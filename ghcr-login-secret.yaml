kubectl create secret docker-registry ghcr-login-secret \
  --docker-server=<your-registry-server> \
  --docker-username=shiri177 \
  --docker-password=<your-PAT> \
  --docker-email=<your-email> \
  --namespace <your-namespace>
